[Definition]
# Triggers when one host produces repeated 404 responses in access.log.
failregex = ^<HOST> - .* "(?:GET|POST|HEAD) .* HTTP/.*" 404 .*

# Keep noise low for common browser misses.
ignoreregex = ^<HOST> - .* "(?:GET|HEAD) /(favicon\.ico|robots\.txt) HTTP/.*" 404 .*
